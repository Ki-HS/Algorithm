#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int n, m;

void solution(vector<int> v, vector<int> temp, int depth) {
	
}


int main() {
	cin >> n >> m;
	vector<int> temp(m), v(m);

	while (n--) {
		cin >> v[n];
	}
	sort(v.begin(), v.end());
	
	
}